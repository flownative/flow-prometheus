services:
  redis:
    image: redis:latest
    ports:
      - "6380:6379"

  php:
    image: php:8.2-cli
    command: tail -f /dev/null
    depends_on:
      - redis
    env_file:
      - .env
    volumes:
      - ./:/opt/project
    working_dir: /opt/project
